[package]
name = "nixos-terraform-provider"
version = "0.1.0"
edition = "2024"

[dependencies]
tf-provider = "0.2.2"
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
async-trait = "0.1"
snafu = "0.8"
openssh = "0.10"
